# .env is gitignored; 

# PostgreSQL
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=cyber_grc

# App secrets (generate  random syou can use node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" )
APP_KEY=change-me-min-16-chars
JWT_SECRET=change-me-min-32-chars
JWT_EXPIRES_IN=2h
